// Legacy import name retained because the public character route already resolves this
// map first. The implementation is now site-wide: every cast ID gets an archive-first
// narrative biography, with finished hand-written life stories taking precedence.
export { allNarrativeCharacterBios as hishiroNarrativeCharacterBios } from "./character-biographies-all";
