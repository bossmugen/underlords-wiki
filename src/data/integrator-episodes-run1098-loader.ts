import "./integrator-cast-run1098-intake";

// Loader module: all-content.ts eagerly imports integrator-episodes*.ts modules.
// Run 1098's actual episode/gag entries are injected by the cast integration module
// so the same reconciled intake pass owns Kaede, Mugen reputation texture,
// Photoshoot public-output structure, and the Break the Bot gag.
export const run1098Loader = [];
