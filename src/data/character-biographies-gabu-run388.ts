import type { CharacterNarrative } from "./character-biographies-narrative";

// Run 388 Gabu material was folded into the canonical deep officer owner
// (`character-bios-officers-core-deep.ts`). Keep this routed module empty so the
// resolver does not duplicate the same person-shaped QA material as a second overlay.
export const run388GabuCharacterBios: Record<string, CharacterNarrative> = {};
