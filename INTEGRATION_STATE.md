# UNDERLORDS WIKI — Integration State

_Last updated: 2026-09-21 — Run 1557 close_

MAIN's `archive-intake/INTEGRATOR_BRANCH_STATE.json` on `bossmugen/underlords:master` is the controlling miner-consumption ledger. MAIN canon locks and resolved identity corrections remain the highest-priority identity/role layer. WIKI owns structured Cast dossiers, Relationships, Episodes, Running Gags, Quotes, Locations, and Gallery/media notes; MAIN owns richer person-first biographies. Historical run-by-run state remains available in Git history; this file is the current handoff snapshot.

## Run 1557 close

The mandatory full census found **51 tracked miner identities = 30 active refs + 21 historical/missing**, with **0 newly discovered, 1 advanced/unconsumed tail at opening, 9 reviewed `pending_publication` families, 0 backlog, and 0 overdue**. Both Club-Only recovery lanes remain current.

Exactly one miner delta was reviewed checkpoint-first from changed `archive-intake/*` handoffs only, with no raw archive re-mining and no miner-branch merge:

- Wall: `a91a0bf8140da382408cfa463186bf964858ff34` → `11af7e03d1ec058b51d8fc77094711b32503a245`

### Material integrated

**Cele / `uh.cele` / stable account `745353665149141144`** now owns a structured WIKI Wall dossier. The useful cumulative read is attachment-heavy Wall regular who repeatedly turns posting into room activity: `Celebrate with me`, the chart bit followed by an immediate `Wait`, and the tiny notification-management command `Get rid of the red dot`. The last line is especially good person texture because a deleted user immediately pushes back with `you say that like its a bad thing`; Cele is apparently both part of the Wall noise and mildly offended by the unread badge generated by it.

That packet was already integrated concurrently on WIKI before this integrator's publication step, in `src/data/integrator-cast-run1557-wall-cele.ts` plus the canonical Cast wiring. This run therefore **deduped by meaning and did not publish a second Cele dossier**.

Held rails remain held: the deleted-user responder is unresolved; a rich message that renders blank in the text lane does not get invented; attachment-heavy presence does not establish `MADE BY` / `CAPTURED BY` / `FEATURING`; the export-time `Underlord` role array is not appointment chronology; earliest surviving Cele activity is not guaranteed origin.

### Public surfaces

**MAIN reader-facing pages changed: 0 intentionally.** This is a bounded Wall behavior/quote packet that strengthens structured Cast and Running Gag texture; it does not justify a receipt-shaped paragraph in a richer MAIN biography.

**WIKI reader frontier: `9c05a2ae1fa586e235824c0bb7dfd87f16729bd8`.** `Build Underlords Wiki` run `35665075029` completed successfully, and `Deploy Underlords Wiki Preview / Pages` run `35665075036` completed successfully against that exact reader SHA. No duplicate reader commit or ceremonial rebuild was created by the integration-state close.

MAIN's latest reader-verification workflow remains inherited Run 1556 workflow `35664776110`: its only `Source + Astro + built-output verification` job failed before any step instantiated (`steps: []`, `runner_id: 0`), so it remains a runner/startup failure rather than a source/Astro/built-output content failure. Run 1557 changed no MAIN reader bytes and therefore did not manufacture another reader build around state-only bookkeeping.

### Consumption handoff

Wall is reviewed and consumed through `11af7e03d1ec058b51d8fc77094711b32503a245` in MAIN's controlling ledger and is **do not reread** unless its miner head advances again. Closing review state is **0 unread pending-review branches, 0 backlog, 0 overdue**. The 9 `pending_publication` families remain reviewed publication/resynthesis debt rather than unread mining.

Existing hard rails remain unchanged: stable IDs control over similar names; role arrays are not appointment chronology; earliest surviving is not guaranteed origin; joke family/romance/governance language stays nonliteral unless separately supported; and `SAID BY` / `POSTED BY` / `MADE BY` / `CAPTURED BY` / `FEATURING` remain distinct.