# UL WIKI integration — Run 840

The controlling MAIN census closes at **51 tracked miner identities = 30 active refs + 21 historical/missing refs**, with **0 newly discovered refs, 0 pending review, 0 backlog, 0 overdue**, and **8 reviewed `pending_publication` families**. Both Club-Only recovery-priority lanes remain current/saturated.

Run 840 reviewed two intake tails checkpoint-first and intake-only: Daycare `e3ce6358… → 3277dbf6…`, then a late Wall mover `bd0a6144… → 8fdb1d14…`. No raw archive was re-mined and no miner branch was merged wholesale.

## Public WIKI result

The reader change is owned by `src/data/integrator-cast-run840-daycare.ts` and its `src/data/all-content.ts` activation.

- **BishopThaGuru:** added the February 2021 self-posted drink/food list and the highly specific `I hate black mirrors but glass ones I can tolerate` preference as ordinary-life / Petty Crimes texture. `East Coast` was already public and remains corroboration rather than a duplicate claim.
- **Gilli:** resolved stable account `531846053179736074` / historical display names `Mr. Streamer` and `Watch_My_Thrust` into canonical Gilli through the `gilli69` account trail. `uwuconner` remains unresolved and is not merged.
- **Alkey:** added the Wall nostalgia-soft ↔ petty-heckler contradiction (`The good ol' days` beside compact roast/complaint behavior). More importantly, a prior WIKI overlay had accidentally created **Meowk** as a second Cast person; Run 840 reconciles Meowk / `itsalkey` back into canonical Alkey, preserving the hard Alkey ≠ Key rail instead of maintaining character mitosis.
- **Cele q18:** two exact positive-set parent IDs were pinned backstage, but the historical ≥4 set cannot yet be provenance-deduped safely, so no arithmetic inflation or new public character claim was made.
- **Lithium_Darko:** one expressive singleton remains no-promote / rotate.

Exact verified/deployed reader SHA: **`3ad4f78225fa47ed390f2aad1ff446c97a6d979a`**.

Verification/deployment:
- `Build Underlords Wiki` run **`34566209526`** — success; build job **`103158752298`** — success.
- `Deploy Underlords Wiki Preview` / Pages run **`34566209523`** — success.
  - build/artifact **`103158751572`** — success;
  - deploy **`103158842770`** — success;
  - report/status **`103158884201`** — success.

## Cross-surface disposition

MAIN reader-facing changes: **0 intentionally**. MAIN already has richer person-first Bishop and Alkey biographies, so these small receipt-scale additions would make those pages more report-like if appended there. The resolved Gilli account bridge is durably locked backstage on MAIN in `archive-intake/IDENTITY_LOCK_RUN840_GILLI.md`.

Daycare is formally consumed through `3277dbf6d91755af75c787a0779eb76d180c6aed`. Wall is reviewed/DNR through `8fdb1d14785635a2db2493c2c33afacacecf33d7` but remains formally pinned at `ce04b5baae00245024d7f24de190630a2d645390` behind the older accepted MAIN Woosung Rich→Ricochet exact-reader production gate. Whiskey remains separately pinned behind the older MAIN Lena chronology gate.
